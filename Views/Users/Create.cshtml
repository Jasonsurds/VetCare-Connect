@{
    Layout = "_DashboardLayout";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="vc-card">
            <div class="vc-card-head">
                <h3 class="vc-card-title">Create user account</h3>
                <a class="vc-card-link" asp-action="Index">← Back to accounts</a>
            </div>
            <div class="vc-card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label-vc">Full name *</label>
                            <input name="name" class="form-control-vc" required />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-vc">Role *</label>
                            <select name="role" class="form-control-vc" asp-items="ViewBag.RoleList"></select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-vc">Username *</label>
                            <input name="userName" class="form-control-vc" required />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-vc">Password *</label>
                            <input name="password" type="password" class="form-control-vc" required minlength="6" />
                            <div class="form-hint">Stored as a secure hash, never plain text.</div>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-vc">Email</label>
                            <input name="email" type="email" class="form-control-vc" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-vc">Contact number</label>
                            <input name="contactNumber" class="form-control-vc" />
                        </div>
                    </div>
                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn-vc">Create account</button>
                        <a class="btn-vc-outline" asp-action="Index">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
